import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Bind to localhost only for security
  // Server options handled via CLI: next dev -H 127.0.0.1
};

export default nextConfig;
